# fig

a simple gcode file viewer.

## building

unless you want to edit the `Cargo.toml` file you have to also pull the `fox-graphics` and `fox-gcode` repositories into the same directory this repo is pulled into.